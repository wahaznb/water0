# Gradle
.gradle/
build/
*/build/
!*/src/main/**/build.gradle.kts
!*/src/androidTest/**/build.gradle.kts

# Local config
local.properties
*.iml
.idea/
*.iws
*.ipr

# OS
.DS_Store
Thumbs.db

# Design sources (IconKitchen export, not needed for build)
IconKitchen-Output.zip

# Curated release packages (sideload APKs, checksums, notes):
# distributed via GitHub Releases, never tracked in git.
release/

# Android
*.apk
*.ap_
*.aab
*.dex
*.class
*.jar
*.so
*.o
*.lo
*.a
*.dll
*.dylib
*.bundle
*.a
*.la
*.lib
*.exp
*.dll
*.dylib
*.bundle

# Logs
*.log

# Hilt
app/build/generated/hilt/

# Keystore
*.jks
*.keystore
keystore.properties

# VS Code
.vscode/
!/.vscode/extensions.json
!/.vscode/settings.json

# Local-only learning notes (never push)
learning-guide/
session-*.md

# Private project docs for owner + AI (never push; see actual shii/00-START-HERE.md)
/actual shii/

# Phone screenshots shared for review (never push; wildcard because
# the folder name contains characters that don't survive literally)
/screenshots */

# ...except the Gradle wrapper jar, which CI needs to run ./gradlew
!gradle-wrapper.jar